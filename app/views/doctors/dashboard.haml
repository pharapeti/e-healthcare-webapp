%h1 Your personalised dashboard

- if @waiting_urgent_request.present?
  = render partial: 'waiting_urgent_request'

- if @upcoming_consultations.present?
  = render partial: 'upcoming_consultations'
  %br
- if @unanswered_questions.present?
  = render partial: 'unanswered_questions'

- if @waiting_urgent_request.blank? && @upcoming_consultations.blank? && @unanswered_questions.blank?
  %i No activity yet
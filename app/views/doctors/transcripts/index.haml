%h3 Your Transcripts

- if @transcripts.present?
  %table.table.table-hover
    %thead
      %tr
        %th Patient
        %th Description
        %th Time of session
        %th

    %tbody
      - @transcripts.each do |transcript|
        %tr
          %td= transcript.chat_room.patient.name           
          %td= transcript.chat_room.request.description
          %td= transcript.chat_room.request.created_at.strftime('%d/%m/%y: %H:%M %p')
          %td= link_to 'Show', doctor_transcript_path(id: transcript)

- else
  %i You have not had any consultations
  
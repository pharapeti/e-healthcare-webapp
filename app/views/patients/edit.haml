%h2 Update Profile

= bootstrap_form_for current_user, url: edit_user_registration_path do |f|
  - if current_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(current_user.errors.count, "error")} prohibited this doctor from being saved:"
      %ul
        - current_user.errors.full_messages.each do |message|
          %li= message
  
  .form-inputs
  = f.text_field :username
  = f.email_field :email
  .form-actions
    = link_to "Back", :back, class: 'btn btn-danger'
    = f.submit 'Save', class: 'btn btn-primary'


%h3 Prescriptions

- if @prescriptions.present?
  %table.table.table-hover
    %thead
      %tr
        %th Doctor
        %th Prescribed at
        %th
    
    %tbody
      - @prescriptions.each do |prescription|
        %tr
          %td= prescription.doctor.name
          %td= prescription.created_at.strftime('%d/%m/%y: %H:%M %p')
          %td= link_to 'Show', prescription_path(id: prescription.id)
- else
  %i You have no prescriptions